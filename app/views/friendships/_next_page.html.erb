<%= render partial: 'friendships/friends', locals: { friends: friends } %>

  <% unless friendships.last_page? %>
    <%= turbo_frame_tag 'next_page', src: friends_user_profile_path(user_id: @user_id, page: friendships.next_page), loading: 'lazy' do %>
      <%= render 'shared/loading' %>
    <% end %>
  <% end %>