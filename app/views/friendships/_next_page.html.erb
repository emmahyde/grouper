<%= render partial: 'friendships/friendships', locals: { friendships: friendships } %>

<% unless friendships.last_page? %>
  <%= turbo_frame_tag 'next_page', src: friends_profile_path(unique_name: @user.unique_name, page: friendships.next_page), loading: 'lazy' do %>
    <%= render 'shared/loading' %>
  <% end %>
<% end %>
