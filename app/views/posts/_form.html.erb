<%= form_with(model: @post) do |form| %>
  <% if @post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% @post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='p-4 border-x border-b border-yellow-600'>
    <%= form.text_area :text, maxlength: "280", placeholder: "Detail Your Labors", class: "w-full resize-none p-2" %>

    <div class='text-right'>
      <%= form.submit "Poast", class: "rounded-2xl ml-full py-1 px-2 bg-yellow-600 text-white" %>
    </div>
  </div>

  
<% end %>
